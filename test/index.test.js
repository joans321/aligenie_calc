const request = require('supertest')
const chai = require('chai')
const assert = chai.assert
const expect = chai.expect


describe('HTTP GET', function() {
    it('Get / should redirect to blog server', function() {
        assert.equal(-1, -1);
        expect(1).to.equal(1);
    });
});




